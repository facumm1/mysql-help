-- Cuenta la cantidad de usuarios
SELECT COUNT(*) FROM users;

-- Suma el campo age de todos los usuarios de la tabla users
SELECT SUM(age) FROM users;

-- Promedio del campo age de todos los usuarios de la tabla users
SELECT AVG(age) FROM users;
