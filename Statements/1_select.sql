-- Selecciona todos los registros de la tabla users
SELECT * from users;

-- Selecciona los campos name de la tabla users
SELECT name FROM users;

-- Selecciona los campos user_id y name de la tabla users
SELECT user_id, name FROM users;